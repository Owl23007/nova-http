{
  "name": "{{name}}",
  "version": "{{projectVersion}}",
  "private": true,
  "description": "基于 Nova 框架的 Node.js 应用",
  "scripts": {
    "dev": "ts-node app.ts",
    "build": "tsc",
    "start": "node dist/app.js"
  },
  "dependencies": {
    "nova-http": "{{frameworkVersionRange}}"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
