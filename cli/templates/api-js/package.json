{
  "name": "{{name}}",
  "version": "{{projectVersion}}",
  "private": true,
  "description": "基于 Nova 框架的 RESTful API 服务",
  "scripts": {
    "dev": "node app.js",
    "start": "node app.js"
  },
  "dependencies": {
    "nova-http": "{{frameworkVersionRange}}"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
